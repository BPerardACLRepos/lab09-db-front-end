import React from 'react';
import { Link } from 'react-router-dom';
import Loading from './Loading.js';
import { addGame } from './ApiUtils.js';

export default class CreatePage extends React.Component {
    render() {
        return (
            <div>

            </div>
        )
    }
}